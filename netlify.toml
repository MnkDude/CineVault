[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"

[functions]
  directory = "src/server"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200